using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerDetector : MonoBehaviour
{
    //TODO: Player should become child object of moving platform
    //TODO: Probably I should create separate empty collider which would detect if player is landed ON platform rather than hitting it from sides.


}
